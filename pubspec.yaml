name: become_a_steel_rat
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  shared_preferences: ^0.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
  - images/
  fonts:
  - family: Comfortaa
    fonts:
    - asset: fonts/Comfortaa-Regular.ttf
  - family: Lobster
    fonts:
    - asset: fonts/Lobster-Regular.ttf
